const CategoryHeader = ({ title }) => (
  <header className="bg-black text-white py-10 text-center text-3xl tracking-widest font-bold">
    {title}
  </header>
);

export default CategoryHeader;
